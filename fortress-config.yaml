#═══════════════════════════════════════════════════════════════════════════════
#  ███████╗ ██████╗ ██████╗ ████████╗██████╗ ███████╗███████╗███████╗
#  ██╔════╝██╔═══██╗██╔══██╗╚══██╔══╝██╔══██╗██╔════╝██╔════╝██╔════╝
#  █████╗  ██║   ██║██████╔╝   ██║   ██████╔╝█████╗  ███████╗███████╗
#  ██╔══╝  ██║   ██║██╔══██╗   ██║   ██╔══██╗██╔══╝  ╚════██║╚════██║
#  ██║     ╚██████╔╝██║  ██║   ██║   ██║  ██║███████╗███████║███████║
#  ╚═╝      ╚═════╝ ╚═╝  ╚═╝   ╚═╝   ╚═╝  ╚═╝╚══════╝╚══════╝╚══════╝
#
#  🏰 Asrar Mared Hub - Fortress Configuration
#  📦 Version: 2.0.0
#  🛡️ License: Cyber Fortress
#  👤 Developer: المارد الرقمي
#  🔐 Security Level: MAXIMUM
#═══════════════════════════════════════════════════════════════════════════════

#───────────────────────────────────────────────────────────────────────────────
# 📋 Project Metadata - بيانات المشروع
#───────────────────────────────────────────────────────────────────────────────
project:
  name: "asrar-mared-hub"
  version: "2.0.0"
  description: "🏰 مركز أسرار المارد الرقمي - قلعة البرمجة الآمنة"
  author: "المارد الرقمي"
  license: "Cyber Fortress"
  repository: "https://github.com/mared/asrar-mared-hub"
  homepage: "https://asrar-mared.dev"
  
  # 📅 تواريخ مهمة
  created: "2025-01-01"
  updated: "2025-10-23"
  
  # 🏷️ تصنيفات
  tags:
    - security
    - fortress
    - automation
    - devops
    - protection

#───────────────────────────────────────────────────────────────────────────────
# 🛡️ Security Configuration - إعدادات الأمان
#───────────────────────────────────────────────────────────────────────────────
security:
  # 🔐 مستوى الحماية
  level: "maximum"  # options: low, medium, high, maximum
  
  # 🚨 وضع التنبيهات
  alerts:
    enabled: true
    methods:
      - email
      - webhook
      - log
    
    # 📧 إعدادات البريد الإلكتروني
    email:
      enabled: true
      recipients:
        - "security@asrar-mared.dev"
        - "admin@asrar-mared.dev"
      smtp:
        host: "smtp.gmail.com"
        port: 587
        secure: true
  
  # 🔍 فحص الملفات الحساسة
  sensitive_files:
    scan_enabled: true
    patterns:
      - "**/*.env"
      - "**/*.key"
      - "**/*.pem"
      - "**/*.p12"
      - "**/secrets.*"
      - "**/credentials.*"
      - "**/*password*"
      - "**/*token*"
    
    # ⚠️ الإجراءات عند الاكتشاف
    action: "block"  # options: warn, block, quarantine
  
  # 🛡️ حماية Git
  git_protection:
    enabled: true
    hooks:
      pre_commit: true
      pre_push: true
      commit_msg: true
    
    # 🚫 منع الـ commits المباشرة
    protect_branches:
      - main
      - master
      - production
  
  # 🔐 تشفير الملفات
  encryption:
    enabled: true
    algorithm: "AES-256-GCM"
    key_rotation_days: 90
    
    # 📁 ملفات تحتاج تشفير
    auto_encrypt:
      - "config/secrets/**"
      - "backups/**/*.backup"
      - "**/*.db"

#───────────────────────────────────────────────────────────────────────────────
# 📦 Backup Configuration - إعدادات النسخ الاحتياطي
#───────────────────────────────────────────────────────────────────────────────
backup:
  # ✅ تفعيل النسخ الاحتياطي
  enabled: true
  
  # 📁 المجلدات المستهدفة
  targets:
    - path: "projects/"
      priority: "high"
    - path: "config/"
      priority: "critical"
    - path: "scripts/"
      priority: "medium"
    - path: "docs/"
      priority: "low"
  
  # ⏰ الجدولة التلقائية
  schedule:
    frequency: "daily"  # options: hourly, daily, weekly, monthly
    time: "02:00"  # 2 صباحاً
    timezone: "Africa/Cairo"
  
  # 💾 التخزين
  storage:
    location: "backups/"
    retention_days: 30  # حذف النسخ الأقدم من 30 يوم
    compression: true
    compression_level: 9  # 0-9 (9 = أقصى ضغط)
    
    # ☁️ رفع للـ Cloud (اختياري)
    cloud_sync:
      enabled: false
      provider: "s3"  # options: s3, azure, gcp
      bucket: "asrar-mared-backups"
  
  # 🔐 حماية النسخ الاحتياطية
  encryption: true
  password_protected: true

#───────────────────────────────────────────────────────────────────────────────
# 📊 Logging Configuration - إعدادات السجلات
#───────────────────────────────────────────────────────────────────────────────
logging:
  # ✅ تفعيل السجلات
  enabled: true
  
  # 📝 مستوى التفاصيل
  level: "info"  # options: debug, info, warn, error, critical
  
  # 📁 مسارات الملفات
  files:
    main: "logs/fortress.log"
    security: "logs/security.log"
    error: "logs/error.log"
    access: "logs/access.log"
  
  # 🔄 التدوير التلقائي
  rotation:
    enabled: true
    max_size: "100MB"  # حجم الملف الأقصى
    max_files: 10  # عدد الملفات المحفوظة
    compress: true
  
  # 📊 التنسيق
  format:
    timestamp: true
    level: true
    source: true
    json: false  # استخدم JSON للسجلات
  
  # 🎯 تصفية السجلات
  filters:
    exclude_patterns:
      - "**/node_modules/**"
      - "**/*.test.js"
      - "**/temp/**"

#───────────────────────────────────────────────────────────────────────────────
# 🚀 Build & Deploy - إعدادات البناء والنشر
#───────────────────────────────────────────────────────────────────────────────
build:
  # 🔧 إعدادات البناء
  output_dir: "dist/"
  source_map: false  # للأمان
  minify: true
  tree_shaking: true
  
  # 🌍 البيئات
  environments:
    development:
      debug: true
      source_map: true
      minify: false
    
    staging:
      debug: true
      source_map: true
      minify: true
    
    production:
      debug: false
      source_map: false
      minify: true
      optimize: "aggressive"
  
  # 📦 Assets
  assets:
    optimize_images: true
    compress_css: true
    compress_js: true

deploy:
  # 🚀 استراتيجية النشر
  strategy: "blue-green"  # options: rolling, blue-green, canary
  
  # ✅ فحوصات ما قبل النشر
  pre_deploy_checks:
    - security_scan
    - tests
    - linting
    - build
  
  # 🌐 البيئات
  targets:
    production:
      url: "https://asrar-mared.dev"
      auto_deploy: false  # يدوي للـ production
    
    staging:
      url: "https://staging.asrar-mared.dev"
      auto_deploy: true

#───────────────────────────────────────────────────────────────────────────────
# 🧪 Testing Configuration - إعدادات الاختبارات
#───────────────────────────────────────────────────────────────────────────────
testing:
  # ✅ تفعيل الاختبارات
  enabled: true
  
  # 🧪 أنواع الاختبارات
  suites:
    unit:
      enabled: true
      coverage_threshold: 80  # نسبة التغطية المطلوبة
      timeout: 5000  # milliseconds
    
    integration:
      enabled: true
      timeout: 10000
    
    e2e:
      enabled: true
      browser: "chromium"
      headless: true
  
  # 📊 تقارير التغطية
  coverage:
    enabled: true
    reporters:
      - html
      - text
      - lcov
    output_dir: "coverage/"
  
  # 🎯 متطلبات النجاح
  success_criteria:
    min_coverage: 80
    max_failures: 0
    max_warnings: 5

#───────────────────────────────────────────────────────────────────────────────
# 🔍 Code Quality - جودة الكود
#───────────────────────────────────────────────────────────────────────────────
code_quality:
  # ✨ ESLint
  linting:
    enabled: true
    rules: "strict"  # options: recommended, strict, custom
    fix_on_save: true
    max_warnings: 0
  
  # 🎨 Prettier
  formatting:
    enabled: true
    auto_format: true
    config:
      semi: true
      singleQuote: true
      tabWidth: 2
      trailingComma: "es5"
  
  # 📝 TypeScript
  type_checking:
    enabled: true
    strict: true
    no_implicit_any: true
  
  # 📊 Complexity Analysis
  complexity:
    enabled: true
    max_cyclomatic: 10
    max_cognitive: 15

#───────────────────────────────────────────────────────────────────────────────
# 🔔 Notifications - الإشعارات
#───────────────────────────────────────────────────────────────────────────────
notifications:
  # 📧 البريد الإلكتروني
  email:
    enabled: true
    events:
      - deployment_success
      - deployment_failure
      - security_alert
      - backup_complete
  
  # 💬 Slack
  slack:
    enabled: false
    webhook_url: "${SLACK_WEBHOOK_URL}"
    channel: "#fortress-alerts"
  
  # 📱 Discord
  discord:
    enabled: false
    webhook_url: "${DISCORD_WEBHOOK_URL}"
  
  # 🔔 Custom Webhooks
  webhooks:
    - name: "custom_monitoring"
      url: "https://monitor.asrar-mared.dev/webhook"
      events:
        - "*"  # جميع الأحداث

#───────────────────────────────────────────────────────────────────────────────
# ⚙️ Advanced Settings - إعدادات متقدمة
#───────────────────────────────────────────────────────────────────────────────
advanced:
  # 🔄 Auto Update
  auto_update:
    enabled: true
    check_frequency: "weekly"
    auto_apply: false  # يدوي للأمان
  
  # 🧹 Cleanup
  cleanup:
    enabled: true
    schedule: "weekly"
    targets:
      - temp_files
      - old_logs
      - cache
      - node_modules_cache
  
  # 🎯 Performance
  performance:
    cache_enabled: true
    parallel_builds: true
    max_workers: 4
  
  # 🌍 Localization
  localization:
    default_language: "ar"
    supported_languages:
      - ar
      - en
    
  # 🎨 UI/UX
  ui:
    theme: "dark"
    colors:
      primary: "#6366f1"
      secondary: "#8b5cf6"
      success: "#10b981"
      warning: "#f59e0b"
      error: "#ef4444"

#───────────────────────────────────────────────────────────────────────────────
# 🔗 Integrations - التكاملات الخارجية
#───────────────────────────────────────────────────────────────────────────────
integrations:
  # 🐙 GitHub
  github:
    enabled: true
    auto_merge_dependabot: false
    label_management: true
  
  # 🐳 Docker
  docker:
    enabled: true
    registry: "ghcr.io"
    image_name: "asrar-mared-hub"
  
  # ☁️ Cloud Providers
  cloud:
    aws:
      enabled: false
      region: "us-east-1"
    
    azure:
      enabled: false
    
    gcp:
      enabled: false

#═══════════════════════════════════════════════════════════════════════════════
# 📝 Configuration Metadata
#═══════════════════════════════════════════════════════════════════════════════
meta:
  config_version: "2.0.0"
  last_updated: "2025-10-23"
  schema_version: "1.0"
  
  # 🔐 Validation
  validation:
    strict_mode: true
    required_fields:
      - project.name
      - security.level
      - backup.enabled
  
  # 📋 Documentation
  docs_url: "https://docs.asrar-mared.dev/config"
  support_url: "https://support.asrar-mared.dev"

#═══════════════════════════════════════════════════════════════════════════════
# 🛡️ Fortress Signature
#═══════════════════════════════════════════════════════════════════════════════
# Generated by: المارد الرقمي
# Protection Level: MAXIMUM
# Status: ACTIVE
# Checksum: SHA256:abc123...
#═══════════════════════════════════════════════════════════════════════════════
